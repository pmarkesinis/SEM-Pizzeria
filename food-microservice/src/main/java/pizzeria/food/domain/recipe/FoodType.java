package pizzeria.food.domain.recipe;

/**
 * Enum representing the type of food from this recipe
 * Used for scalability in case the chain wants to add different food types
 */
public enum FoodType {
    PIZZA
}
